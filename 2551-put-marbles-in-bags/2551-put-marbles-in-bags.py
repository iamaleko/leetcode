class Solution:
  def putMarbles(self, weights: List[int], k: int) -> int:
    h = []
    n = len(weights)
    for i in range(1, n):
      s.append((weights[i-1] + weights[i], i))
    heapify(h)
    s = 0
    splits
    while s < k:

      s += 1
    print(splits)

        